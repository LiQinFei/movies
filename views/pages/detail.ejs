<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <%- include("../includes/head.ejs") %>

</head>
<body>

 
	
<div class="container">
	<h2><%= movie.title %></h2>

  <div class="row">
  <div class="col-md-8">
  	<img src="<%= movie.poster %>" alt="..." class="img-thumbnail">
  </div>
  <div class="col-md-4">

  	<p class="text-center">标题：<%= movie.title %></p>
  	<p class="text-center">作者：<%= movie.doctor %></p>
  	<p class="text-center">国家：<%= movie.country %></p>
  	<p class="text-center">描述：<%= movie.summary %></p>
  	<p class="text-center">年代：<%= movie.year %></p>
  	<p class="text-center">更新时间：<%= movie.meta.updateAt %></p>
  </div>
</div>
</div>

</body>
</html>